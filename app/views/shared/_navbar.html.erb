<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
  <%= link_to "PostBook", root_path, class:"navbar-brand font-weight-bold"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to "Posts", root_path, class:"nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Create Post", new_post_path, class:"nav-link" %>
      </li>
      <% 
        if session[:user_id]
            @user=User.find_by(id: session[:user_id])
        %>
          <% if !@user %>
              <li class="nav-item">
                  <%= link_to "Sign In", signin_path, class:"nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Sign Up", signup_path, class:"nav-link" %>
              </li>
          <%end%>
          
      <%end%>
        
        
    </ul>
    <ul class="navbar-nav ml-auto">
      
        <li class="nav-item mt-2">
          <%= link_to @user.name, profile_path(@userprofile), class:"nav-link current-user" %>
        </li>
        <li class="nav-item">
          <%= link_to "Settings", edit_path(@user), class:"nav-link" %>
        </li>
        <li class="nav-item">
            <%= button_to "Logout", logout_path, class:"nav-link p-0 logout-btn mt-2", method: :delete %>
        </li>
    </ul>
  </div>
</nav>